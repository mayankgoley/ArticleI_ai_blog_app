# Core Django framework
whitenoise==6.6.0
Django>=5.0.0

# YouTube video processing
yt-dlp>=2024.1.0

# Configuration management
python-decouple==3.8

# Automatic Speech Recognition (ASR) dependencies
# faster-whisper: Lightweight Whisper implementation for production deployment
# Works on Render without system dependencies or GPU requirements
faster-whisper>=1.0.0

# ONNX Runtime: Inference engine for faster-whisper
# Lightweight, CPU-optimized, no CUDA required
onnxruntime==1.20.1

# FFmpeg Python bindings for audio extraction
# Note: Requires ffmpeg system package to be installed
ffmpeg-python==0.2.0

# Production server
gunicorn==21.2.0